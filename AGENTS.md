# 暦司｜あなたの予定秘書 開発ガイド

## プロジェクトの目的

「日付や締切までの距離をつかみにくい人」が、予定を迷わず登録し、今日やることをすぐ理解できるアプリを作る。

## 成果物

- 公開用ファイルは `index.html`。
- フレームワークやビルド工程を使わない、単一HTMLのWebアプリとして保つ。
- データは端末内の `localStorage` に保存し、任意でGitHub Gistと同期する。

## 開発時の優先順位

1. 操作が分かりやすく、登録ミスを本人が確認・修正できること。
2. PCとスマートフォンの両方で使えること。
3. データを失いにくいこと。
4. 外部AI APIや有料サービスに依存しないこと。
5. 既存データとの互換性を守ること。

## UIと文章

- 日本語で、短く、やさしい表現を使う。
- ボタン名だけで次の動作が分かるようにする。
- OCRや日付解析の結果は確定前に編集できるようにする。
- エラー時に行き止まりを作らず、手入力や再試行へ案内する。
- 色だけに意味を持たせず、文字やアイコンも併用する。

## 実装上の注意

- ユーザーの既存タスクを壊す `localStorage` のキー変更は避ける。
- GitHubトークンやGist IDをコードへ埋め込まない。
- HTMLへユーザー入力を表示するときは `escapeHTML` を通す。
- カメラ、音声、通知はブラウザごとの差が大きいため、非対応時の代替手段を必ず残す。
- 通知はアプリを開いている間だけ動作する制約を、誤解のない表現で案内する。
- CDN依存を追加するときは、用途、オフライン時の挙動、代替手段を確認する。
- タスクを変更するたびに `updatedAt` を更新し、削除時は削除記録を残す。
- Gistへ送信する直前にリモート側を取得し、更新時刻が新しい予定を優先して統合する。
- Gistの `hizuke-tasks.json` は旧版との互換性のため配列形式を保ち、削除記録などは `hizuke-sync-meta.json` に分ける。
- 同期やバックアップ復元の前には、現在の状態を端末内へ自動退避する。

## カメラ・OCRの方針

- 撮影画像は長辺を適切なサイズへ縮小し、向き、明るさ、コントラストを整えてからOCRへ渡す。
- 認識中は進捗を表示する。
- 認識した文章を編集する確認画面を挟み、その後に日付解析する。
- 日本語の写真はカラー画像と弱く補正した画像を異なる文字配置モードで認識し、候補を比較できるようにする。
- 手書きの予定表では、数字と日付・時刻記号に絞った認識候補も用意する。
- 読み取り失敗時も、同じ画面から手入力できるようにする。
- OCR候補に有効な日付・時刻がない場合は、写真を表示したまま予定名・日付・時刻を入力できる画面へ自動で切り替える。
- 精度改善では、実際に失敗した画像や言い回しを個人情報を除いて再現し、回帰確認する。

## 音声入力の方針

- 音声認識の結果は予定へ即時登録せず、編集できる文字起こし確認画面を挟む。
- ブラウザが複数の認識候補を返した場合は、利用者が候補を選べるようにする。
- 「予定名・日付・時間」の順で、短く区切って話す例を画面に示す。
- 全角数字、漢数字、午前・午後など、音声認識で生じやすい表記揺れを日付解析側で吸収する。
- 音声認識が使えない場合や権限が拒否された場合は、文字入力へ案内する。

## くり返し予定の方針

- 毎日、毎週の複数曜日、毎月の日付、毎月の第○曜日・最終曜日に対応する。
- くり返し予定は次回分だけを保存し、完了または「次回へ送る」で次の該当日へ進める。
- カレンダー表示では、保存データを増やさずに表示範囲内の将来分を展開する。
- 前日通知を設定できるようにし、通知がアプリを開いている間だけ動く制約を併記する。
- 既存タスクでは `recurrence` がない状態を「くり返さない」として扱う。

## 変更後の確認

- JavaScriptに構文エラーがないこと。
- テキスト、カメラ、アルバム、音声の各入口が開くこと。
- 写真の切り抜き枠を移動・拡大縮小でき、選んだ範囲だけがOCRへ渡ること。
- 予定の追加、編集、完了、削除が動くこと。
- 削除直後に「元に戻す」で復元できること。
- バックアップの書き出し、読み込み、自動退避からの復元が動くこと。
- 期限切れ、今日、今週、あとで、日付未定、完了の分類が正しいこと。
- 過去の日付、重複予定、日付なし通知を保存前に確認・防止できること。
- リストとカレンダーを切り替えられること。
- 既存のlocalStorageデータを読み込めること。
- 画面幅が狭いスマートフォンでも、主要ボタンが隠れないこと。
- カメラ権限拒否、OCRライブラリ未読込、文字を認識できない場合にも手入力へ進めること。
- 音声の文字起こしを修正でき、別候補の選択と再録音ができること。
- 「九月十九日 午後三時」「９月１９日 １５：３０」を正しく日付・時刻へ変換できること。
- 毎日、毎週複数曜日、毎月の日付、第○曜日、最終曜日の次回日を正しく計算できること。
- くり返し予定の完了・スキップで、今日より後の次回日へ進むこと。
- 前日通知と当日通知が同じ日に重複発火しないこと。

## 公開

- GitHub Pagesでは `index.html` を公開する。
- 公開前にローカルで確認し、公開後はPCとスマートフォンの両方で最新版が表示されるか確認する。
- キャッシュで旧版が出る場合は、再読み込みだけでなくシークレットウィンドウでも確認する。
